#!/usr/bin/env sh
set -e
DIR="$(cd "$(dirname "$0")" && pwd)"
if command -v gradle >/dev/null 2>&1; then
  exec gradle -p "$DIR" "$@"
else
  echo "Gradle is not installed. Please install Gradle to build Android artifacts." >&2
  exit 1
fi
